# A conda environment for running and developing DeeSi
#
# You might want to adapt the name to your preferences.
#
# Install and activate as follows:
#   $ conda env create -f deesi_dev.yaml
#   $ conda activate deesi_dev
#
# Further packages can be added interactively anytime later on.
#
# https://docs.conda.io/projects/conda/en/stable/user-guide/tasks/manage-environments.html
#
name: deesi_dev
channels:
  - conda-forge
dependencies:
  - python=3.11
  - coverage
  - pytest-cython
  - jupyter
  - line_profiler
  - more-itertools
  - mypy
  - pip
  - pyeda
  - pytest
  - pytest-xdist
  - rattler-build
  - sage
  - setuptools_scm
  - snakeviz
  - sphinx
  - sphinx-book-theme
  - viztracer
